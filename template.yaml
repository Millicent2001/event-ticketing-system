AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31

Description: >
  Event Ticketing System
  Serverless application using AWS Lambda, API Gateway, and DynamoDB.

Globals:
  Function:
    Timeout: 3
    LoggingConfig:
      LogFormat: JSON

Resources:

  # =========================
  # Hello World Lambda
  # =========================
  HelloWorldFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: hello_world/
      Handler: app.lambda_handler
      Runtime: python3.14
      Architectures:
        - x86_64
      Events:
        HelloWorld:
          Type: Api
          Properties:
            Path: /hello
            Method: get

  # =========================
  # Register Attendee Lambda
  # =========================
  RegisterAttendeeFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: register_attendee/
      Handler: app.lambda_handler
      Runtime: python3.14
      Architectures:
        - x86_64
      Policies:
        - DynamoDBWritePolicy:
            TableName: !Ref RegistrationsTable
      Events:
        RegisterAttendee:
          Type: Api
          Properties:
            Path: /events/{id}/register
            Method: post

  # =========================
  # List Events Lambda
  # =========================
  ListEventsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: list_events/
      Handler: app.lambda_handler
      Runtime: python3.14
      Architectures:
        - x86_64
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref EventsTable
      Events:
        ListEvents:
          Type: Api
          Properties:
            Path: /events
            Method: get

  # =========================
  # Get Registration Lambda
  # =========================
  GetRegistrationFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: get_registration/
      Handler: app.lambda_handler
      Runtime: python3.14
      Architectures:
        - x86_64
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref RegistrationsTable
      Events:
        GetRegistration:
          Type: Api
          Properties:
            Path: /registrations/{id}
            Method: get

  # =========================
  # Events DynamoDB Table
  # =========================
  EventsTable:
    Type: AWS::Serverless::SimpleTable
    Properties:
      TableName: Events
      PrimaryKey:
        Name: eventId
        Type: String

  # =========================
  # Registrations DynamoDB Table
  # =========================
  RegistrationsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: Registrations
      BillingMode: PAY_PER_REQUEST

      AttributeDefinitions:
        - AttributeName: eventId
          AttributeType: S
        - AttributeName: registrationId
          AttributeType: S
        - AttributeName: email
          AttributeType: S

      KeySchema:
        - AttributeName: eventId
          KeyType: HASH
        - AttributeName: registrationId
          KeyType: RANGE

      GlobalSecondaryIndexes:
        - IndexName: EmailIndex
          KeySchema:
            - AttributeName: email
              KeyType: HASH
          Projection:
            ProjectionType: ALL

# =========================
# Outputs
# =========================
Outputs:

  # =========================
  # Hello World API
  # =========================
  HelloWorldApi:
    Description: API Gateway endpoint URL for Hello World function
    Value: !Sub
      - "https://${ServerlessRestApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/Prod/hello/"
      - {}

  # =========================
  # List Events API
  # =========================
  ListEventsApi:
    Description: API Gateway endpoint URL for List Events function
    Value: !Sub
      - "https://${ServerlessRestApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/Prod/events/"
      - {}

  # =========================
  # Register Attendee API
  # =========================
  RegisterAttendeeApi:
    Description: API Gateway endpoint URL for Register Attendee function
    Value: !Sub
      - "https://${ServerlessRestApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/Prod/events/{id}/register"
      - {}

  # =========================
  # Get Registration API
  # =========================
  GetRegistrationApi:
    Description: API Gateway endpoint URL for Get Registration function
    Value: !Sub
      - "https://${ServerlessRestApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/Prod/registrations/{id}"
      - {}

  # =========================
  # Lambda Function ARNs
  # =========================
  HelloWorldFunction:
    Description: Hello World Lambda Function ARN
    Value: !GetAtt HelloWorldFunction.Arn

  ListEventsFunction:
    Description: List Events Lambda Function ARN
    Value: !GetAtt ListEventsFunction.Arn

  RegisterAttendeeFunction:
    Description: Register Attendee Lambda Function ARN
    Value: !GetAtt RegisterAttendeeFunction.Arn

  GetRegistrationFunction:
    Description: Get Registration Lambda Function ARN
    Value: !GetAtt GetRegistrationFunction.Arn

  # =========================
  # IAM Roles
  # =========================
  HelloWorldFunctionIamRole:
    Description: IAM Role created for Hello World function
    Value: !GetAtt HelloWorldFunctionRole.Arn

  ListEventsFunctionIamRole:
    Description: IAM Role created for List Events function
    Value: !GetAtt ListEventsFunctionRole.Arn

  RegisterAttendeeFunctionIamRole:
    Description: IAM Role created for Register Attendee function
    Value: !GetAtt RegisterAttendeeFunctionRole.Arn

  GetRegistrationFunctionIamRole:
    Description: IAM Role created for Get Registration function
    Value: !GetAtt GetRegistrationFunctionRole.Arn